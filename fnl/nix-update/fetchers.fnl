;;; Define the fetchers' updaters
(local fetchers
  {;; Github
   :fetchFromGitHub
   (fn [body]
     :todo)

   ;; GitLab
   :fetchFromGitLab
   (fn [body]
     :todo)

   ;; Fetch GIT
   :fetchgit
   (fn [body]
     :todo)

   ;; Fetch URL
   :fetchurl
   (fn [body]
     :todo)

   ;; Fetch ZIP
   :fetchzip
   (fn [body]
     :todo)

   ;; EmacsWiki
   :compileEmacsWikiFile
   (fn [body]
     :todo)

   ;; PyPi
   :fetchPypi
   (fn [body]
     :todo)})

fetchers
